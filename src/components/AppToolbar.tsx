import { SECTORS, type Sector } from "../sp500";
type Tab = "All" | Sector;
interface Props { activeTab: Tab; onTabChange: (tab: Tab) => void; count: number; searchQuery: string; onSearchQueryChange: (query: string) => void; }
function SearchIcon(){return <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><circle cx="7" cy="7" r="5.25" stroke="currentColor" strokeWidth="1.4"/><path d="m11 11 3.5 3.5" stroke="currentColor" strokeWidth="1.4" strokeLinecap="round"/></svg>}
export default function AppToolbar({activeTab,onTabChange,count,searchQuery,onSearchQueryChange}:Props){
 const tabs:Tab[]=["All",...SECTORS];
 return <header className="apple-shell pt-7 pb-4"><div className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="m-0 text-[34px] font-bold leading-[41px] text-[#1a1a1a]">Green500</h1><p className="m-0 text-[15px] font-medium text-[#727272]">{count} companies · S&amp;P 500 materiality profiles</p></div><label className="flex h-11 min-w-[280px] items-center gap-2 rounded-full bg-white px-4 border border-black/[0.08] shadow-[0_2px_8px_rgba(0,0,0,0.06)]"><span className="text-[#86868b]"><SearchIcon/></span><input type="search" value={searchQuery} onChange={e=>onSearchQueryChange(e.target.value)} placeholder="Search company or ticker" aria-label="Search companies" className="min-w-0 flex-1 bg-transparent text-[14px] outline-none"/></label></div><nav className="mt-4 flex gap-1 overflow-x-auto pb-1" aria-label="GICS sectors">{tabs.map(tab=><button key={tab} type="button" onClick={()=>onTabChange(tab)} className="h-9 shrink-0 rounded-full px-4 text-[13px] font-medium" style={{border:"none",cursor:"pointer",background:activeTab===tab?"#1d1d1f":"#ffffff",color:activeTab===tab?"#fff":"#1d1d1f"}}>{tab}</button>)}</nav></header>;
}